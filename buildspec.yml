version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - apt-get install python3-sphinx
  build:
    commands:
      - cd docs/
      - make clean html

artifacts:
  files:
    - build*/**
